## EMQX Log Configuration
## =====================

## Console Handler - Primary logging to stdout
console_handler {
  enable = true
  level = debug
  formatter = text
}

## File Handler - Disabled for stdout only
file_handlers.default {
  enable = false
  level = warning
  file = "/opt/emqx/log/emqx.log"
}